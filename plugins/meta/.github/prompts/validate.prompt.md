---
mode: agent
description: Validate this marketplace's structure, manifests, generated mirrors, and license hygiene.
---
<!-- AUTO-GENERATED by scripts/generate.mjs — do not edit by hand. Source: commands/validate.md -->


Validate the marketplace and report problems. Run `node scripts/generate.mjs --check`
and additionally verify:

1. **Manifests**: `.claude-plugin/marketplace.json` parses and every `plugins[].source`
   path exists. Each `plugins/*/.claude-plugin/plugin.json` parses and has `name`/`version`.
2. **Registry sync**: every plugin directory is listed in `marketplace.json` and versions match.
3. **Generated mirrors in sync**: `.github/plugin/marketplace.json` and each
   `plugins/*/.github/plugin.json` match what `scripts/generate.mjs` would produce.
4. **Skills**: every `skills/*/SKILL.md` has valid frontmatter with `name` matching its directory.
5. **License hygiene**: every vendored directory (one carrying a non-root `LICENSE`) has a
   matching entry in `THIRD_PARTY_NOTICES.md`, and no secrets are committed (only `${ENV_VAR}`
   placeholders may appear in any `.mcp.json`).

Report a concise pass/fail summary with file paths for any failures.
